services:
  - type: web
    name: weather-next-day-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main_app:app --host 0.0.0.0 --port $PORT
    plan: free
